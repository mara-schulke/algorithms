#[allow(dead_code)]
#[allow(unused_variables)]
pub fn insertion_sort(arr: &mut [u8]) {
	// for i in 0..arr.len() {
	// 	let temp: u8 = arr[i];
	// 	let mut j: usize = i - 1;

	// 	while j >= 0 && arr[j] > temp {
	// 		arr[j + 1] = arr[j];
	// 		j -= 1;
	// 	}
	// 	arr[j + 1] = temp;
	// }
}
